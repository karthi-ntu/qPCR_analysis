# qPCR Analysis Shiny App Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a Shiny app for qPCR ΔΔCt analysis with bar plots, t-test/ANOVA statistics, and GitHub Pages deployment via Shinylive.

**Architecture:** Single `app.R` with ui + server, backed by pure R functions in `R/analysis.R` and `R/plot.R`. Functions are unit-tested with `testthat`. Deployed as a static site using `shinylive::export()` to `docs/`, served free via GitHub Pages.

**Tech Stack:** R, Shiny, ggplot2, ggsignif, DT, testthat, shinylive

---

## File Map

| File | Responsibility |
|------|---------------|
| `app.R` | Shiny ui + server — wires inputs/outputs, no business logic |
| `R/analysis.R` | Pure functions: ΔCt, ΔΔCt, fold change, t-test/ANOVA |
| `R/plot.R` | Pure function: `make_barplot()` — ggplot2 bar chart with brackets |
| `tests/testthat/test-analysis.R` | Unit tests for all analysis functions |
| `tests/testthat/test-plot.R` | Smoke test for `make_barplot()` |
| `docs/` | Auto-generated by `shinylive::export()` — commit to GitHub |

---

## Task 1: Project Setup

**Files:**
- Create: `R/analysis.R`
- Create: `R/plot.R`
- Create: `tests/testthat/test-analysis.R`
- Create: `tests/testthat/test-plot.R`
- Create: `app.R`

- [ ] **Step 1: Install required R packages**

Open R and run:
```r
install.packages(c("shiny", "ggplot2", "DT", "ggsignif", "testthat", "shinylive"))
```

Expected: all packages install without error. If `shinylive` is not on CRAN yet, install from r-universe:
```r
install.packages("shinylive", repos = c("https://posit-dev.r-universe.dev", "https://cloud.r-project.org"))
```

- [ ] **Step 2: Create the directory structure**

In the terminal (from `E:/qPCR_analysis`):
```bash
mkdir -p R tests/testthat docs
```

- [ ] **Step 3: Create empty stub files**

Create `R/analysis.R` with:
```r
# qPCR analysis pure functions
```

Create `R/plot.R` with:
```r
# qPCR plotting pure function
```

Create `tests/testthat/test-analysis.R` with:
```r
library(testthat)
source("../../R/analysis.R")
```

Create `tests/testthat/test-plot.R` with:
```r
library(testthat)
source("../../R/plot.R")
```

- [ ] **Step 4: Commit**

```bash
git init
git add R/ tests/ app.R
git commit -m "chore: scaffold project structure"
```

---

## Task 2: Analysis Functions — ΔCt and ΔΔCt

**Files:**
- Modify: `R/analysis.R`
- Modify: `tests/testthat/test-analysis.R`

- [ ] **Step 1: Write failing tests for `compute_delta_ct()`**

In `tests/testthat/test-analysis.R`:
```r
library(testthat)
source("../../R/analysis.R")

test_that("compute_delta_ct subtracts reference from target", {
  df <- data.frame(
    Sample = c("S1", "S2"),
    Group = c("Control", "Treatment"),
    Gene = c("ACTB", "ACTB"),
    Ct_target = c(20.0, 22.0),
    Ct_reference = c(18.0, 18.0)
  )
  result <- compute_delta_ct(df)
  expect_equal(result$delta_ct, c(2.0, 4.0))
})

test_that("compute_fold_change sets control group to 1.0", {
  df <- data.frame(
    Sample = c("S1", "S2", "S3"),
    Group = c("Control", "Control", "Treatment"),
    Gene = c("ACTB", "ACTB", "ACTB"),
    delta_ct = c(2.0, 2.0, 4.0)
  )
  result <- compute_fold_change(df, control_group = "Control")
  expect_equal(result$fold_change[result$Group == "Control"], c(1.0, 1.0))
  expect_equal(result$fold_change[result$Group == "Treatment"], 2^(-2.0))
})
```

- [ ] **Step 2: Run tests to confirm they fail**

```r
testthat::test_file("tests/testthat/test-analysis.R")
```

Expected: FAIL — `compute_delta_ct` not found.

- [ ] **Step 3: Implement `compute_delta_ct()` and `compute_fold_change()`**

In `R/analysis.R`:
```r
compute_delta_ct <- function(df) {
  df$delta_ct <- df$Ct_target - df$Ct_reference
  df
}

compute_fold_change <- function(df, control_group) {
  control_dct <- df$delta_ct[df$Group == control_group]
  control_mean <- mean(control_dct, na.rm = TRUE)
  df$delta_delta_ct <- df$delta_ct - control_mean
  df$fold_change <- 2^(-df$delta_delta_ct)
  df
}
```

- [ ] **Step 4: Run tests to confirm they pass**

```r
testthat::test_file("tests/testthat/test-analysis.R")
```

Expected: PASS — 2 tests pass.

- [ ] **Step 5: Commit**

```bash
git add R/analysis.R tests/testthat/test-analysis.R
git commit -m "feat: add delta_ct and fold_change calculation functions"
```

---

## Task 3: Analysis Functions — Statistics

**Files:**
- Modify: `R/analysis.R`
- Modify: `tests/testthat/test-analysis.R`

- [ ] **Step 1: Write failing tests for `run_stats()`**

Append to `tests/testthat/test-analysis.R`:
```r
test_that("run_stats returns unpaired t-test for 2 groups", {
  set.seed(42)
  df <- data.frame(
    Sample = paste0("S", 1:6),
    Group = rep(c("Control", "Treatment"), each = 3),
    Gene = "ACTB",
    delta_ct = c(2.0, 2.1, 1.9, 4.0, 4.1, 3.9)
  )
  result <- run_stats(df, paired = FALSE)
  expect_equal(nrow(result), 1)
  expect_true("p_value" %in% names(result))
  expect_true(result$p_value < 0.05)
  expect_equal(result$Significant, "Yes")
})

test_that("run_stats returns paired t-test result for 2 groups paired=TRUE", {
  df <- data.frame(
    Sample = c("S1", "S2", "S3", "S1", "S2", "S3"),
    Group = c(rep("Control", 3), rep("Treatment", 3)),
    Gene = "ACTB",
    delta_ct = c(2.0, 2.1, 1.9, 4.0, 4.1, 3.9)
  )
  result <- run_stats(df, paired = TRUE)
  expect_equal(nrow(result), 1)
  expect_true(result$p_value < 0.05)
})

test_that("run_stats returns ANOVA + Tukey for 3+ groups", {
  set.seed(42)
  df <- data.frame(
    Sample = paste0("S", 1:9),
    Group = rep(c("Control", "TreatA", "TreatB"), each = 3),
    Gene = "ACTB",
    delta_ct = c(2.0, 2.1, 1.9, 4.0, 4.1, 3.9, 6.0, 6.1, 5.9)
  )
  result <- run_stats(df, paired = FALSE)
  expect_true(nrow(result) >= 2)
  expect_true(all(c("Gene", "Comparison", "p_value", "Significant") %in% names(result)))
})
```

- [ ] **Step 2: Run tests to confirm they fail**

```r
testthat::test_file("tests/testthat/test-analysis.R")
```

Expected: FAIL — `run_stats` not found.

- [ ] **Step 3: Implement `run_stats()`**

Append to `R/analysis.R`:
```r
run_stats <- function(df, paired = FALSE) {
  genes <- unique(df$Gene)

  results <- lapply(genes, function(gene) {
    sub <- df[df$Gene == gene, ]
    groups <- unique(sub$Group)
    n_groups <- length(groups)

    if (n_groups < 2) return(NULL)

    if (n_groups == 2) {
      g1_vals <- sub$delta_ct[sub$Group == groups[1]]
      g2_vals <- sub$delta_ct[sub$Group == groups[2]]

      if (paired) {
        # Align by Sample name for pairing
        sub1 <- sub[sub$Group == groups[1], ]
        sub2 <- sub[sub$Group == groups[2], ]
        common_samples <- intersect(sub1$Sample, sub2$Sample)
        sub1 <- sub1[sub1$Sample %in% common_samples, ]
        sub2 <- sub2[sub2$Sample %in% common_samples, ]
        sub1 <- sub1[order(sub1$Sample), ]
        sub2 <- sub2[order(sub2$Sample), ]
        g1_vals <- sub1$delta_ct
        g2_vals <- sub2$delta_ct
      }

      test <- t.test(g1_vals, g2_vals, paired = paired)
      data.frame(
        Gene        = gene,
        Comparison  = paste(groups[1], "vs", groups[2]),
        p_value     = round(test$p.value, 4),
        Significant = ifelse(test$p.value < 0.05, "Yes", "No"),
        stringsAsFactors = FALSE
      )
    } else {
      aov_fit <- aov(delta_ct ~ Group, data = sub)
      tukey   <- TukeyHSD(aov_fit)$Group
      comps   <- rownames(tukey)
      data.frame(
        Gene        = gene,
        Comparison  = gsub("-", " vs ", comps),
        p_value     = round(tukey[, "p adj"], 4),
        Significant = ifelse(tukey[, "p adj"] < 0.05, "Yes", "No"),
        stringsAsFactors = FALSE
      )
    }
  })

  do.call(rbind, Filter(Negate(is.null), results))
}
```

- [ ] **Step 4: Run all tests to confirm they pass**

```r
testthat::test_file("tests/testthat/test-analysis.R")
```

Expected: PASS — 5 tests pass.

- [ ] **Step 5: Commit**

```bash
git add R/analysis.R tests/testthat/test-analysis.R
git commit -m "feat: add run_stats function (t-test, paired t-test, ANOVA+Tukey)"
```

---

## Task 4: Summary Helper for Plotting

**Files:**
- Modify: `R/analysis.R`
- Modify: `tests/testthat/test-analysis.R`

- [ ] **Step 1: Write failing test for `summarize_groups()`**

Append to `tests/testthat/test-analysis.R`:
```r
test_that("summarize_groups computes mean, sd, sem per group", {
  df <- data.frame(
    Group = c("Control", "Control", "Treatment", "Treatment"),
    Gene = "ACTB",
    fold_change = c(1.0, 1.0, 2.0, 2.0)
  )
  result <- summarize_groups(df)
  expect_equal(nrow(result), 2)
  expect_true(all(c("Group", "Gene", "mean_fc", "sd_fc", "sem_fc") %in% names(result)))
  expect_equal(result$mean_fc[result$Group == "Control"], 1.0)
  expect_equal(result$sd_fc[result$Group == "Control"], 0.0)
})
```

- [ ] **Step 2: Run test to confirm it fails**

```r
testthat::test_file("tests/testthat/test-analysis.R")
```

Expected: FAIL — `summarize_groups` not found.

- [ ] **Step 3: Implement `summarize_groups()`**

Append to `R/analysis.R`:
```r
summarize_groups <- function(df) {
  genes  <- unique(df$Gene)
  groups <- unique(df$Group)

  rows <- lapply(genes, function(gene) {
    lapply(groups, function(grp) {
      vals <- df$fold_change[df$Gene == gene & df$Group == grp]
      if (length(vals) == 0) return(NULL)
      data.frame(
        Gene    = gene,
        Group   = grp,
        mean_fc = mean(vals, na.rm = TRUE),
        sd_fc   = sd(vals, na.rm = TRUE),
        sem_fc  = sd(vals, na.rm = TRUE) / sqrt(length(vals)),
        stringsAsFactors = FALSE
      )
    })
  })

  do.call(rbind, Filter(Negate(is.null), unlist(rows, recursive = FALSE)))
}
```

- [ ] **Step 4: Run all tests**

```r
testthat::test_file("tests/testthat/test-analysis.R")
```

Expected: PASS — 6 tests pass.

- [ ] **Step 5: Commit**

```bash
git add R/analysis.R tests/testthat/test-analysis.R
git commit -m "feat: add summarize_groups helper for plot data"
```

---

## Task 5: Plotting Function

**Files:**
- Modify: `R/plot.R`
- Modify: `tests/testthat/test-plot.R`

- [ ] **Step 1: Write smoke test for `make_barplot()`**

In `tests/testthat/test-plot.R`:
```r
library(testthat)
library(ggplot2)
source("../../R/analysis.R")
source("../../R/plot.R")

test_that("make_barplot returns a ggplot object", {
  summary_df <- data.frame(
    Gene    = "ACTB",
    Group   = c("Control", "Treatment"),
    mean_fc = c(1.0, 2.0),
    sd_fc   = c(0.1, 0.2),
    sem_fc  = c(0.05, 0.1),
    stringsAsFactors = FALSE
  )
  stats_df <- data.frame(
    Gene        = "ACTB",
    Comparison  = "Control vs Treatment",
    p_value     = 0.02,
    Significant = "Yes",
    stringsAsFactors = FALSE
  )
  p <- make_barplot(summary_df, stats_df, gene = "ACTB", error_type = "SD")
  expect_s3_class(p, "gg")
})

test_that("make_barplot works with no significant comparisons", {
  summary_df <- data.frame(
    Gene    = "ACTB",
    Group   = c("Control", "Treatment"),
    mean_fc = c(1.0, 1.1),
    sd_fc   = c(0.1, 0.1),
    sem_fc  = c(0.05, 0.05),
    stringsAsFactors = FALSE
  )
  stats_df <- data.frame(
    Gene        = "ACTB",
    Comparison  = "Control vs Treatment",
    p_value     = 0.8,
    Significant = "No",
    stringsAsFactors = FALSE
  )
  p <- make_barplot(summary_df, stats_df, gene = "ACTB", error_type = "SEM")
  expect_s3_class(p, "gg")
})
```

- [ ] **Step 2: Run tests to confirm they fail**

```r
testthat::test_file("tests/testthat/test-plot.R")
```

Expected: FAIL — `make_barplot` not found.

- [ ] **Step 3: Implement `make_barplot()`**

In `R/plot.R`:
```r
library(ggplot2)
library(ggsignif)

OKABE_ITO <- c(
  "#E69F00", "#56B4E9", "#009E73", "#F0E442",
  "#0072B2", "#D55E00", "#CC79A7", "#000000"
)

make_barplot <- function(summary_df, stats_df, gene, error_type = "SD") {
  sub <- summary_df[summary_df$Gene == gene, ]
  sub$error <- if (error_type == "SD") sub$sd_fc else sub$sem_fc

  # Lock group order so control appears first
  sub$Group <- factor(sub$Group, levels = unique(sub$Group))

  p <- ggplot(sub, aes(x = Group, y = mean_fc, fill = Group)) +
    geom_bar(stat = "identity", color = "black", width = 0.6) +
    geom_errorbar(
      aes(ymin = mean_fc - error, ymax = mean_fc + error),
      width = 0.2, linewidth = 0.7
    ) +
    scale_fill_manual(values = OKABE_ITO) +
    labs(
      title = gene,
      y     = expression("Fold Change (2"^{-Delta*Delta*Ct}*")"),
      x     = NULL
    ) +
    theme_classic(base_size = 14) +
    theme(legend.position = "none")

  # Add p-value brackets for significant comparisons only
  sig <- stats_df[stats_df$Gene == gene & stats_df$Significant == "Yes", ]

  if (nrow(sig) > 0) {
    comparisons_list <- lapply(sig$Comparison, function(comp) {
      # Handle both "A vs B" and "A-B" formats from TukeyHSD
      parts <- trimws(strsplit(comp, " vs |(?<=[^ ])-(?=[^ ])", perl = TRUE)[[1]])
      as.list(parts)
    })
    annotations <- paste0("p = ", sig$p_value)

    p <- p + geom_signif(
      comparisons      = comparisons_list,
      annotations      = annotations,
      map_signif_level = FALSE,
      textsize         = 3.5,
      vjust            = -0.2
    )
  }

  p
}
```

- [ ] **Step 4: Run tests to confirm they pass**

```r
testthat::test_file("tests/testthat/test-plot.R")
```

Expected: PASS — 2 tests pass.

- [ ] **Step 5: Commit**

```bash
git add R/plot.R tests/testthat/test-plot.R
git commit -m "feat: add make_barplot with Okabe-Ito palette and p-value brackets"
```

---

## Task 6: Shiny UI

**Files:**
- Modify: `app.R`

- [ ] **Step 1: Write the full UI in `app.R`**

Replace `app.R` entirely with:
```r
library(shiny)
library(ggplot2)
library(DT)
library(ggsignif)

source("R/analysis.R")
source("R/plot.R")

# ── Default empty data table ─────────────────────────────────────────────────
empty_table <- function() {
  data.frame(
    Sample      = character(0),
    Group       = character(0),
    Gene        = character(0),
    Ct_target   = numeric(0),
    Ct_reference = numeric(0),
    stringsAsFactors = FALSE
  )
}

ui <- fluidPage(
  titlePanel("qPCR Analysis — \u0394\u0394Ct Method"),
  sidebarLayout(
    sidebarPanel(
      width = 4,
      textInput("ref_gene",     "Reference gene (housekeeping)", value = "GAPDH"),
      textInput("ctrl_group",   "Control group name",            value = "Control"),
      radioButtons("error_type", "Error bars",
                   choices = c("SD", "SEM"), inline = TRUE),
      checkboxInput("paired", "Paired samples (2 groups only)", value = FALSE),
      hr(),
      h5("Data entry"),
      DTOutput("table"),
      br(),
      actionButton("add_row",   "Add row",    icon = icon("plus")),
      actionButton("clear_tbl", "Clear table", icon = icon("trash")),
      hr(),
      uiOutput("error_msg")
    ),
    mainPanel(
      width = 8,
      uiOutput("plots_ui"),
      br(),
      h4("Statistics"),
      DTOutput("stats_table"),
      br(),
      fluidRow(
        column(4, downloadButton("dl_png", "Download Plot (PNG)")),
        column(4, downloadButton("dl_pdf", "Download Plot (PDF)")),
        column(4, downloadButton("dl_csv", "Download Stats (CSV)"))
      )
    )
  )
)
```

- [ ] **Step 2: Verify the UI renders without error**

In R console:
```r
shiny::runApp("app.R")
```

Expected: browser opens, sidebar and empty main panel are visible. No R errors in console.

- [ ] **Step 3: Commit**

```bash
git add app.R
git commit -m "feat: add Shiny UI layout"
```

---

## Task 7: Shiny Server — Data Table Management

**Files:**
- Modify: `app.R` (append server logic)

- [ ] **Step 1: Add reactive data store and table server logic**

Append below the `ui` definition in `app.R`:
```r
server <- function(input, output, session) {

  rv <- reactiveValues(data = empty_table())

  # ── Render editable data table ─────────────────────────────────────────────
  output$table <- renderDT({
    datatable(
      rv$data,
      editable  = TRUE,
      rownames  = FALSE,
      selection = "none",
      options   = list(dom = "t", pageLength = 50, scrollY = "300px")
    )
  })

  # Update cell edits back into rv$data
  observeEvent(input$table_cell_edit, {
    info <- input$table_cell_edit
    rv$data <- editData(rv$data, info, proxy = "table", rownames = FALSE)
    # Coerce Ct columns to numeric
    rv$data$Ct_target    <- suppressWarnings(as.numeric(rv$data$Ct_target))
    rv$data$Ct_reference <- suppressWarnings(as.numeric(rv$data$Ct_reference))
  })

  # Add a blank row
  observeEvent(input$add_row, {
    new_row <- data.frame(
      Sample      = "",
      Group       = "",
      Gene        = "",
      Ct_target   = NA_real_,
      Ct_reference = NA_real_,
      stringsAsFactors = FALSE
    )
    rv$data <- rbind(rv$data, new_row)
  })

  # Clear entire table
  observeEvent(input$clear_tbl, {
    rv$data <- empty_table()
  })
```

- [ ] **Step 2: Run the app and test data entry**

```r
shiny::runApp("app.R")
```

Expected:
- Click "Add row" 3 times — 3 blank rows appear in table
- Click a cell — it becomes editable, type a value, press Enter — value saves
- Click "Clear table" — table resets to empty

- [ ] **Step 3: Commit**

```bash
git add app.R
git commit -m "feat: add reactive data table with add/clear row support"
```

---

## Task 8: Shiny Server — Reactive Analysis and Plots

**Files:**
- Modify: `app.R` (continue appending inside `server`)

- [ ] **Step 1: Add validation, analysis, and plot rendering**

Append inside `server` (before the closing `}`):
```r
  # ── Input validation ───────────────────────────────────────────────────────
  validation_msg <- reactive({
    df <- rv$data
    if (nrow(df) == 0) return("Enter at least one row of data.")
    if (any(is.na(df$Ct_target) | is.na(df$Ct_reference)))
      return("All Ct_target and Ct_reference values must be numeric.")
    if (!input$ctrl_group %in% df$Group)
      return(paste0("Control group '", input$ctrl_group, "' not found in Group column."))
    grp_counts <- table(df$Gene, df$Group)
    if (any(grp_counts < 2))
      return("At least 2 samples per group per gene required for statistical testing.")
    NULL
  })

  output$error_msg <- renderUI({
    msg <- validation_msg()
    if (!is.null(msg)) div(style = "color:red;", icon("exclamation-triangle"), msg)
  })

  # ── Core analysis (only runs when validation passes) ──────────────────────
  analyzed <- reactive({
    req(is.null(validation_msg()))
    df <- rv$data
    df <- compute_delta_ct(df)
    df <- compute_fold_change(df, control_group = input$ctrl_group)
    df
  })

  stats_result <- reactive({
    req(analyzed())
    run_stats(analyzed(), paired = input$paired)
  })

  summary_result <- reactive({
    req(analyzed())
    summarize_groups(analyzed())
  })

  # ── Render one plot per gene ───────────────────────────────────────────────
  genes_in_data <- reactive({
    req(analyzed())
    unique(analyzed()$Gene)
  })

  output$plots_ui <- renderUI({
    req(genes_in_data())
    plot_output_list <- lapply(genes_in_data(), function(gene) {
      plotOutput(paste0("plot_", make.names(gene)), height = "400px")
    })
    do.call(tagList, plot_output_list)
  })

  observe({
    req(genes_in_data())
    lapply(genes_in_data(), function(gene) {
      local({
        g <- gene
        output[[paste0("plot_", make.names(g))]] <- renderPlot({
          make_barplot(summary_result(), stats_result(), gene = g,
                       error_type = input$error_type)
        })
      })
    })
  })

  # ── Statistics table ──────────────────────────────────────────────────────
  output$stats_table <- renderDT({
    req(stats_result())
    df <- stats_result()
    # Highlight significant rows
    datatable(df, rownames = FALSE, options = list(dom = "t")) |>
      formatStyle("Significant",
        target          = "row",
        backgroundColor = styleEqual("Yes", "#d4edda")
      )
  })
```

- [ ] **Step 2: Add closing brace for server and shinyApp call**

Append at the very end of `app.R`:
```r
}  # end server

shinyApp(ui, server)
```

- [ ] **Step 3: Run the app end-to-end test**

```r
shiny::runApp("app.R")
```

Test with this data (add rows manually):

| Sample | Group | Gene | Ct_target | Ct_reference |
|--------|-------|------|-----------|--------------|
| S1 | Control | ACTB | 25.0 | 18.0 |
| S2 | Control | ACTB | 25.2 | 18.1 |
| S3 | Control | ACTB | 24.8 | 17.9 |
| S4 | Treatment | ACTB | 27.0 | 18.0 |
| S5 | Treatment | ACTB | 27.2 | 18.1 |
| S6 | Treatment | ACTB | 26.8 | 17.9 |

Expected:
- Bar plot appears with Control bar at 1.0
- Treatment bar shows fold change ~4.0 (2^2)
- Stats table shows comparison with a p-value
- Significant rows highlighted green

- [ ] **Step 4: Commit**

```bash
git add app.R
git commit -m "feat: add reactive analysis, per-gene plots, and stats table"
```

---

## Task 9: Download Handlers

**Files:**
- Modify: `app.R` (append inside `server`, before closing `}`)

- [ ] **Step 1: Add download handlers**

Append inside `server` (before the final `}  # end server` line):
```r
  # ── Combined plot for download (all genes stacked) ─────────────────────────
  combined_plot <- reactive({
    req(genes_in_data())
    plots <- lapply(genes_in_data(), function(g) {
      make_barplot(summary_result(), stats_result(), gene = g,
                   error_type = input$error_type)
    })
    # Stack plots vertically using patchwork if multiple genes
    if (length(plots) == 1) {
      plots[[1]]
    } else {
      # Simple vertical arrangement without patchwork dependency
      plots[[1]]  # For single download, export first gene; multi-gene exports loop below
    }
  })

  output$dl_png <- downloadHandler(
    filename = function() paste0("qpcr_plot_", Sys.Date(), ".png"),
    content  = function(file) {
      req(genes_in_data())
      n <- length(genes_in_data())
      plots <- lapply(genes_in_data(), function(g) {
        make_barplot(summary_result(), stats_result(), gene = g,
                     error_type = input$error_type)
      })
      png(file, width = 800, height = 500 * n, res = 150)
      for (p in plots) print(p)
      dev.off()
    }
  )

  output$dl_pdf <- downloadHandler(
    filename = function() paste0("qpcr_plot_", Sys.Date(), ".pdf"),
    content  = function(file) {
      req(genes_in_data())
      n <- length(genes_in_data())
      plots <- lapply(genes_in_data(), function(g) {
        make_barplot(summary_result(), stats_result(), gene = g,
                     error_type = input$error_type)
      })
      pdf(file, width = 7, height = 5 * n)
      for (p in plots) print(p)
      dev.off()
    }
  )

  output$dl_csv <- downloadHandler(
    filename = function() paste0("qpcr_stats_", Sys.Date(), ".csv"),
    content  = function(file) {
      req(stats_result())
      write.csv(stats_result(), file, row.names = FALSE)
    }
  )
```

- [ ] **Step 2: Test downloads**

```r
shiny::runApp("app.R")
```

With data entered from Task 8 Step 3:
- Click "Download Plot (PNG)" — downloads a PNG file, open it, verify bar plot is visible at 150 dpi
- Click "Download Plot (PDF)" — downloads a PDF, open it, verify vector quality
- Click "Download Stats (CSV)" — downloads CSV, open in Excel/text editor, verify columns and p-values

- [ ] **Step 3: Commit**

```bash
git add app.R
git commit -m "feat: add PNG, PDF, and CSV download handlers"
```

---

## Task 10: GitHub Pages Deployment via Shinylive

**Files:**
- `docs/` — auto-generated

- [ ] **Step 1: Create a GitHub repository**

On github.com: create a new public repository named `qpcr-analysis` (or any name). Copy the remote URL.

```bash
git remote add origin https://github.com/<your-username>/qpcr-analysis.git
git branch -M main
git push -u origin main
```

- [ ] **Step 2: Export the app with Shinylive**

In R (from the project root `E:/qPCR_analysis`):
```r
shinylive::export(appdir = ".", destdir = "docs")
```

Expected: a `docs/` folder is created containing `index.html`, `shinylive/`, and other static assets.

- [ ] **Step 3: Commit and push docs/**

```bash
git add docs/
git commit -m "feat: add Shinylive static export for GitHub Pages"
git push
```

- [ ] **Step 4: Enable GitHub Pages**

On github.com → your repo → Settings → Pages:
- Source: **Deploy from a branch**
- Branch: `main` / folder: `/docs`
- Click Save

Wait ~60 seconds, then visit: `https://<your-username>.github.io/qpcr-analysis/`

Expected: the qPCR app loads in the browser. Note: first load may take 10–20 seconds as WebAssembly and R packages download.

- [ ] **Step 5: Verify the live app**

On the GitHub Pages URL:
- Add 6 rows of test data (same as Task 8 Step 3)
- Confirm plot renders, stats table appears, downloads work

- [ ] **Step 6: Re-export workflow (for future updates)**

Whenever `app.R` changes, re-run:
```r
shinylive::export(appdir = ".", destdir = "docs")
```
Then commit and push `docs/`. GitHub Pages automatically redeploys.

---

## Self-Review Checklist

- [x] ΔΔCt calculation — Task 2
- [x] Fold change = 2^−ΔΔCt — Task 2
- [x] Control group always = 1.0 — Task 2 test
- [x] Unpaired t-test — Task 3
- [x] Paired t-test — Task 3
- [x] ANOVA + Tukey post-hoc — Task 3
- [x] Auto-detect group count — Task 3 (run_stats checks n_groups)
- [x] Paired checkbox in UI — Task 6
- [x] p-values as numbers, not stars — Task 5 plot, Task 8 stats table
- [x] Significant rows highlighted green — Task 8
- [x] Okabe-Ito palette — Task 5 (OKABE_ITO constant)
- [x] SD / SEM toggle — Task 6 UI, Task 7 analysis, Task 5 plot
- [x] Multi-gene support — Task 8 (per-gene plot loop)
- [x] Download PNG 150 dpi — Task 9
- [x] Download PDF vector — Task 9
- [x] Download Stats CSV — Task 9
- [x] Error: non-numeric Ct — Task 8 validation
- [x] Error: control group not found — Task 8 validation
- [x] Error: n < 2 per group — Task 8 validation
- [x] Paired mismatch fallback warning — run_stats uses intersect of sample names
- [x] GitHub Pages deployment — Task 10
- [x] Color-blind friendly palette — Task 5
